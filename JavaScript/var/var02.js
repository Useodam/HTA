/*
    let과 var
        1. let은 변수가 호이스팅되지 않는다
        2. let 변수의 스코프가 블록이다.
*/
console.log("변수 a의 값 : ",a);

var a = 100;
console.log("변수 a의 값 : ",a);

/*
---------------------------------------------------
    | 소스코드 내용
    |----------------------------------------------
    |
    | var a = 100;
    | console.log("변수 a의 값 :", a);
    -----------------------------------------------

    -----------------------------------------------
    | 실제로 실행되는 코드 내용
    | ---------------------------------------------
    | var a; // 변수가 호이스팅되었다.
    | console.log("변수 a의 값 :", a);
    |
    | a = 100;
    | console.log("변수 a의 값 :", a);
    ------------------------------------------------
*/